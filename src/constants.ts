export const BAHT = '฿' as const
export const SMALL_IMAGE_LENGTH = 256 as const
export const LARGE_IMAGE_LENGTH = 1024 as const
export const SMALL_IMAGE_SIZE = `${SMALL_IMAGE_LENGTH}x${SMALL_IMAGE_LENGTH}` as const
export const LARGE_IMAGE_SIZE = `${LARGE_IMAGE_LENGTH}x${LARGE_IMAGE_LENGTH}` as const
